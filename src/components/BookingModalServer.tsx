import FormModal from "@/components/FormModal";

const BookingModalServer = () => {
  return (
    <FormModal
      table="equipment-booking"
      type="create"
    />
  );
};

export default BookingModalServer; 